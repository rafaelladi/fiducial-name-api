server:
  servlet:
    context-path: /name-api
spring:
  application:
    name: name-api
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/names
  jpa:
    hibernate:
      ddl-auto: update